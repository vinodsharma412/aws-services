# NSE Stock Dashboard — Pure Lambda (no FastAPI, no Mangum, no EC2)
# Target runtime: Python 3.12 (Lambda) / 3.14+ (local dev with uvicorn)
#
# FastAPI removed: API Gateway routes directly to Lambda functions.
# boto3/botocore pre-installed in Lambda runtime (included here for local dev).

# ── AWS SDK ────────────────────────────────────────────────────────────────────
boto3>=1.34.144                  # DynamoDB, S3, SQS, SNS, SSM, Cognito, Comprehend,
botocore>=1.34.144               # Rekognition, Translate, StepFunctions, SES, AppConfig

# ── Configuration ─────────────────────────────────────────────────────────────
pydantic>=2.8.2
pydantic-settings>=2.4.0

# ── Stock data ─────────────────────────────────────────────────────────────────
yfinance>=0.2.43
pandas>=2.2.2

# ── HTTP client (news sentiment, Yahoo Finance browser impersonation) ──────────
httpx>=0.27.0
curl_cffi>=0.7.3

# ── Scraping (Lambda worker) ──────────────────────────────────────────────────
beautifulsoup4>=4.12.0           # HTML parsing — works in Lambda (no Playwright)

# ── AWS X-Ray distributed tracing ─────────────────────────────────────────────
aws-xray-sdk>=2.14.0             # Auto-traces boto3, httpx, all Lambda calls

# ── Auth (Cognito replaces passlib/jose — kept for DynamoDB legacy users) ─────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==3.2.2

# ── Local development only (not deployed to Lambda) ───────────────────────────
# Install these locally if you want to run the old FastAPI app:
#   pip install fastapi uvicorn[standard] mangum starlette python-multipart
